# ---------------------------------------------------------------------------
# Module 7: Policymaker Dashboard -- environment template
# ---------------------------------------------------------------------------

# Base URL of Module 6's Priority & Recommendation FastAPI service.
# Should match whatever host/port uvicorn is running on. The dashboard
# reads this via `import.meta.env.VITE_API_BASE_URL`.
VITE_API_BASE_URL=http://localhost:8006